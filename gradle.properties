# Opt-out flag for bundling Kotlin standard library
kotlin.stdlib.default.dependency=false

# IntelliJ Platform 插件构建暂关 Configuration Cache，避免偶发失效
org.gradle.configuration-cache=false
org.gradle.caching=true
